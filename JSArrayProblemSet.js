var reverse = [];

function printReverse(input) {
    for (var i = input.length - 1; i >= 0; i--) {
        reverse.push(input[i]);
    }
    reverse.forEach(function (reverse)) {
        console.log(reverse);
    }
}